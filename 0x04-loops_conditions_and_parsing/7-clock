#!/usr/bin/env bash
# Bash script that displays the time for 12 hours and 59 minutes
min=60
hr=0
while :
do
	if (("$min" == 60)); then
		echo "Hour: " $hr
		min=1
		((hr++))
	elif (("$hr" == 12 && "$min" == 59)); then
		break
	else
		echo $min
		((min++))
	fi
done
